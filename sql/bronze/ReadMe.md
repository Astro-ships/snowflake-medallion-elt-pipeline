# Bronze Layer

The Bronze layer is built from the RAW schema.

Purpose:
- Normalize column names.
- Apply light standardization (e.g., uppercase text fields).
- Preserve the source data with minimal transformations.
- Provide consistent input for the Silver layer.